# specify path to lqx.conf or leave blank
# default is the same as LqxCore
#lqx_conf=/home/evan82/.lqxcore/lqx.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
